# REQ-180-PROD-RELEASE-01 — Frozen Task Brief

Implement the frozen cumulative `v1.8.0-1` release capability for v1.7 + v1.8, with predecessor `v1.6.1-1` and product candidate `7b73997dca8fb104ee51d1353183f8eda8532583`.

`release_execution_sha` remains `TO_FREEZE_AFTER_IMPLEMENTATION_MERGE`; final target inventory remains deferred. Production is not authorized. The exact allowed and forbidden boundaries are in `task.yaml`; the end gate is `WEB_REVIEW_MERGE_AND_READ_ONLY_PROJECT`.
